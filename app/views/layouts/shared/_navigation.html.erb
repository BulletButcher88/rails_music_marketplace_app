<nav class="navbar navbar-expand-lg nav-bg-color mg-bt rm-pd nav-shadow">
  <div class="collapse navbar-collapse mg-bt " id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li class="nav-link">
        <%= link_to '', root_path, class: 'nav-link fas fa-home icon-style'%></li>
      </li>
      <li class="nav-link">
        <%= link_to "Post an item", new_item_path, class: 'nav-link' %>
      </li>
    </ul>
    <ul class="nav push-right">
      <% if user_signed_in? %>
      <div class="align-name">
        <%= current_user.first_name %>&nbsp;<%=current_user.last_name%>
      </div>
        <li class="dropdown">
          <a class="dropdown-toggle nav-link arrow-caret" data-toggle="dropdown">
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu" role="menu">
            <li><%= link_to 'Profile', edit_user_registration_path, class: "text" %></li>
            <li><%= link_to 'Log out', destroy_user_session_path, method: :delete, class: "text" %></li>
          </ul>
        </li>
        <% else %>
        <li><%= link_to 'Log In', new_user_session_path, class: 'nav-link' %></li>
        <li><%= link_to 'Sign Up', new_user_registration_path, class: 'nav-link' %></li>
      <% end %>
    </ul>
  </div>
  <div class="collapse navbar-collapse" class="categories-nav">
      <ul class="navbar-nav mr-auto">
        <% @categories.each do |category| %>   
          <li><%= link_to category.item_category, category_path(category.id),  class: "nav-link" %></li>
        <% end %>
      </ul>
    </div>
<% flash.each do |key, value| %>
    <%= value %>
  <% end %>
</nav>